apiVersion: apps/v1
kind: Deployment
metadata:
  name: gke-web
  namespace: gke-dev
  labels:
    app: gke-web
    env: dev
spec:
  replicas: 2
  selector:
    matchLabels:
      app: gke-web
  template:
    metadata:
      labels:
        app: gke-web
        env: dev
    spec:
      containers:
        - name: gke-web
          image: asia-northeast3-docker.pkg.dev/gcp-assign-494513/gke-gitops-images/gke-web:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: gke-config
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
